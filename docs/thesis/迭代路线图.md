# label-sync 迭代路线图

> 起草: 2026-05-15
> 上次更新: 2026-06-09（追加「工程待办总览」章节，来自全项目 review）
> 答辩时间: 2026-11 / 2026-12 (至少半年后)
> 作者: jxwu1 + [同学]

记录从「v1 全量回测刚跑完」到「答辩稿定稿」之间的演进路径。每个阶段写明背景、动机、主要工作、预期产出与可能的坑。

> **进度提示（2026-05-30）**: 实际进度大幅超前计划。原定 6 月的 **Phase 1（PG 迁移 + `app/` 重构 + 员工/采购订单进 DB + Lead Time 实测）代码层全部已完成**，提前约一个月。当前重心可前移到 Phase 2（算法扩展）。Phase 1 仅剩「论文 §7.5 落档」与「pg_dump 自动备份」两项收尾，已并入对应章节标注。

---

## 时间线总览

```
2026-05  ████████████████████  ✅ Phase 1 完成: PG 迁移 + app/ 重构 + 员工/采购进 DB + LT 实测（提前一月）
2026-06  ████████░░░░░░░░░░░░  Phase 2: 算法扩展 (Holt-Winters + TSB)  ← 前移
2026-07  ████████░░░░░░░░░░░░  Phase 3: ML 集成 (Prophet, 可选 LSTM)  ← 前移
2026-08  ████████░░░░░░░░░░░░  Phase 4: 监控 + 多视图敏感性 + 自动备份  ← 前移
2026-09  ████████░░░░░░░░░░░░  Phase 5: 论文终稿 + 答辩准备（含 §7.5 补写）
2026-10  ████████░░░░░░░░░░░░  buffer: 同学第 4 章 + 导师反馈循环
2026-11  ████████░░░░░░░░░░░░  buffer + 答辩
```

每个 Phase 1 个月，故意留出 buffer。中小型团队（2 人本科）的真实节奏远比"打鸡血"的快得多。

**实际情况**: Phase 1 在 5 月底（计划是 6 月整月）就完成了代码层，证明上面的预留 buffer 是对的。前移后整体多出约 1 个月余量，主要灌进 Phase 5 的论文写作和同学协作（历史经验里最容易拖的环节）。

---

## 现状（2026-05-30）

**项目状态**:
- DB: 已从 SQLite 迁移到 **shared PostgreSQL（PostGIS 17）**，Coolify `infrastructure/shared-pg17` 容器；SQLite 文件已退役
- 代码结构: 已从平铺重构为 `app/` 包结构（`app/models.py` / `app/routes/` / `app/services/`）
- ORM 表: 26 张，含本轮新建的 `employees` `attendance_records` `leave_records` `public_holidays` `special_days` `inactive_periods`（员工+考勤）和 `purchase_orders` `purchase_order_lines`（采购订单主档）
- Lead Time 实测: `app/services/purchase.py::compute_supplier_lead_times()` 已实现
- 采购定价表（老板报价表）: 已上线（commit `0486170`）
- 本地开发循环: `dev.ps1` 起本地 PG + 热重载，`pull_prod_db.py` 灌真实数据，调试不再上服务器

**已完成但路线图原标「待办」（本次更新已勾掉）**:
- ✅ Phase 1 PG 迁移、`app/` 重构、Phase 1.6 全部（员工/考勤/采购订单进 DB + LT 实测）

**仍未解决 / 待办**:
- ⬜ 论文 §7.5「数据持久化层规整」章节未写（代码已落地，等补文字）
- ⬜ pg_dump 自动备份 cron 未配（路线图 Phase 4.4，可提前）
- ⬜ v3 backtest（PG 版本）是否重跑、分数 vs v2 漂移验证 — 待确认
- ⬜ 本地 PG 派生列（last_purchase_unit_price / supplier_id / master_stock_price_eur）为空，依赖它们的功能本地测不了，LT 实测需线上数据验证
- ⬜ 论文图未画（matplotlib）
- ⬜ 作者 B 章节占位待同学填充

---

## 工程待办总览（2026-06-09 全项目 review 产出）

> 来源：后端（架构/质量/运维）+ 前端（模板/JS）五路评估 + shared.js 统一实操中的发现。
> 与论文 Phase 无直接绑定的工程债集中在这里，做完打勾。
> 已完成：✅ 前端工具层统一（shared.js: esc/byId/qs/apiFetch，14 文件去重，fc0d72a）

### P0 — 价值兑现 + 防盲飞（各 2-3 天）

- [ ] **forecast_output 表 + dashboard 预测卡片** — 回测已验证（EmpiricalQuantile 胜出）但无 UI 出口，
  算法投入对补货决策零收益；方案见 `2026-05-12-forecast-and-backtest.md` §3.7
- [ ] **cron 失败告警接通** — heartbeat 判定已有但告警渠道没接，抓取/预测失败时盲飞

### P1 — 工程质量收口（各 0.5-2 天，独立可发布）

- [ ] **后端统一错误处理** — 8+ 路由裸 `except Exception` 无日志（`routes/attendance.py` 7 处、
  `routes/history.py:15`）；3 处 `except OSError: pass` 静默吞错（`routes/inventory.py:88`、
  `routes/stockpile.py:50`）→ Flask errorhandler + logging 收口
- [ ] **apiFetch 迁移** — shared.js 封装已就位，25 个 JS 文件散装 fetch 逐文件迁；
  优先修两个真 bug：inventory.js 预览失败不清缓存、purchase.js 解析失败 rows 残留
- [ ] **修 e2e 烟雾测试** — `test_smoke_nav.py`/`test_nav_lazy_load.py` 在 main 上已坏
  （auth 后没更新被 302 到登录墙 + 引用已下线的 sales_analytics/transfer 页）；
  修法：`page.request.post("/login")` + nav 列表从 Alpine store 动态取
- [ ] **requirements.txt 锁版本** — 目前全裸名，每次 Coolify build 赌上游兼容

### P2 — 结构债（无依赖，有重构窗口时整体做）

- [ ] 拆 `app/routes/analytics.py`（983 行单蓝图 → sku/restock/forecast 子蓝图）
- [ ] 拆 `app/services/attendance.py`（794 行混 CRUD/假期/报表/导入四职责）
- [ ] route→service 越层收口（20+ 处路由直接 ORM：`dashboard.py:119-285`、
  `inventory.py:298-353`、`analytics.py:87`、`admin.py:45`）
- [ ] 拆三个千行 JS（attendance 1340 / restock 1248 / purchase 996 → 按渲染/动作/数据切）
- [ ] pnl 面板头 Jinja 宏化（同一结构 12 个 partial 里重复 50+ 次）
- [ ] Alembic 25 个迁移补 downgrade（目前全单向，生产回滚难）

### P3 — 运维硬化（部分并入 Phase 4）

- [ ] **scraper 单点消除**（风险最高）— Task Scheduler + Chrome session 绑死本地机，
  失败无重试；短期加重试+告警，中期评估迁容器内 cron
- [ ] 日志中央化（docker logs 无聚合，Loki 轻量方案优先）→ 并入 Phase 4 监控
- [ ] 业务监控指标（healthcheck 只测 HTTP 200；导入延迟/预测新鲜度无暴露）→ 并入 Phase 4
- [ ] DB 备份 RTO/RPO 明确化 + 恢复演练 → 与 Phase 4.4 pg_dump 自动备份同做

### P4 — 小项（顺手做）

- [ ] 折叠控件补 `aria-expanded` + 键盘支持（`_page_admin.html:60`、`_page_history.html:54` onclick 改 Alpine）
- [ ] 2 处 `x-html` 审核，能换 `x-text` 就换（`_page_dashboard.html:91`、`_page_main.html:181`）
- [ ] 搜索框补关联 label（`_page_restock.html:33`、`_page_history.html:14`）
- [ ] components.css 收敛（`.btn--primary`/`.btn-primary` 双命名统一，重复规则约减 40%）
- [ ] `store.js:126` setInterval 不清理 + `window.__xxx` 全局函数收口（可并入 P2 千行 JS 拆分）
- [ ] 测试盲区：异常路径集成测试、task_state 并发、upload→import→analytics 端到端链

### 建议节奏

本周 P0 两项 → 之后 P1 逐项穿插 → 重构窗口进 P2 → 服务器侧有空时 P3。

---

## Phase 1 (2026-06): SQLite → PostgreSQL 迁移 — ✅ 已完成（2026-05，提前一月）

> **状态（2026-05-30）**: 代码层 100% 完成。PG 迁移走的是 Coolify shared PostgreSQL（PostGIS 17）而非计划里的自建 `postgres:16-alpine` 容器；`app/` 包重构、员工/考勤进 DB、采购订单主档化、Lead Time 实测全部落地。**仅剩论文 §7.5 落档（移到 Phase 5）和 pg_dump 自动备份（移到 Phase 4.4）两项收尾。** 下方原始计划内容保留作论文写作素材。

### 故事背景

「现在 SQLite 跑得挺好，为什么要迁？」—— 答辩评委可能这么问。论文 §2.4 给的理由是「数据量小、单写源、运维简单」，这是真话，但答辩讲完后下一句通常是「以后呢」。

「以后」可能发生的事：
1. 客户扩到 2-3 家公司（每家一个 SKU 池），需要 schema 隔离 + 跨公司聚合
2. 后端 worker 数从 1 增到多个（gunicorn `-w 4`），SQLite 写锁打架
3. 引入 cron 任务跑夜间 backtest / 报表，跟前端读冲突
4. 加 ML 模型训练管道，可能需要 R 或 Spark 读 DB

任一情况发生时迁 PG 都不可避免。论文「为支持未来扩展，本系统设计了 SQLite → PostgreSQL 迁移路径」比临时迁要扎实得多。

### 主要工作

#### 1.1 设计阶段（3 天）

- 选 PG 容器（`postgres:16-alpine`）vs 托管（Hetzner 没有 RDS）→ 选容器
- 选连接池（pgbouncer vs SQLAlchemy 内置）→ 简单起见用 SQLAlchemy 内置 pool
- 决定数据迁移工具（pgloader vs 手动 dump/load）→ pgloader（已支持 SQLite → PG）
- 决定测试策略（testcontainers vs 保留 SQLite for tests）→ **保留 SQLite for tests + 加少量 PG smoke tests**

#### 1.2 代码改造（5-7 天）

逐文件审查 SQLite-only 语法：

| 文件 | 问题 | 修改 |
|---|---|---|
| forecast_data.py | 无 | 无 |
| backtest_service.py | 无（NumPy 纯计算） | 无 |
| categorizer.py | `_fetch_last_sale_at` 用 `func.max` 跨 DB | 无 |
| analytics_service.py | 可能用 strftime | grep 后改 `EXTRACT(WEEK FROM ...)` 等 |
| stockpile_db.py | PRAGMA WAL（SQLite 专属） | 加 `if dialect=='sqlite'` 守护 |
| spike_*.py | `'__null__' || ie.id` 用 SQLite || 拼接 | PG 也支持 `||` 但需 cast → `'__null__' || ie.id::text` |
| alembic versions | `server_default=text("(datetime('now','localtime'))")` | 改 `CURRENT_TIMESTAMP` 或加 dialect 分支 |
| alembic env.py | DB URL 写死 sqlite:// | 走 env var `DATABASE_URL` |
| models.py | DB URL | 同上 |
| config.py | `stockpile_db` path 字段 | 加 `database_url` 字段 |
| Dockerfile / compose | 单容器 | 加 postgres service + depends_on |
| requirements.txt | - | 加 psycopg2-binary |

#### 1.3 数据迁移（1-2 天）

```bash
# 1. 起 PG 容器
docker compose up -d postgres

# 2. 用 pgloader 把 SQLite 全部表迁到 PG
docker run --rm --network=label-sync_default \
  -v $(pwd)/stockpile.db:/data/stockpile.db \
  dimitri/pgloader pgloader sqlite:///data/stockpile.db \
  postgresql://label_sync:pass@postgres/label_sync

# 3. 跑 alembic upgrade head (校验 schema 对齐)
docker compose exec label-sync alembic upgrade head

# 4. sanity check: row counts 对得上, 抽样查询结果一致
```

#### 1.4 测试改造（2 天）

`tests/` 已 708 测试，目标是**继续用 SQLite + 加少量 PG 集成测试**：
- pure 函数测试不变（无 DB 依赖）
- DB-bound 测试保留 SQLite mock（速度快）
- 新增 `tests/test_pg_integration.py` 跑几个关键 path（alembic upgrade / 简单 CRUD / 复杂 query）用 docker-compose 起 PG

#### 1.5 部署改造（2 天）

- Coolify 加 postgres service（用 Coolify 提供的 Postgres 模板）
- 改 docker-compose 加 depends_on
- 改 env 变量 `DATABASE_URL`
- 备份策略：pg_dump cron → 推到 Hetzner Storage Box

#### 1.6 员工 + 采购订单 schema 规整（5-7 天，跟 PG 迁移并行）

**动机**: 当前项目有两个数据持久化层游离在 DB 之外：

1. **员工 + 考勤**: 全部存 `attendance/` 目录 JSON 文件
   - `employees.json` 员工主档
   - `metadata.json` 系统元数据
   - `holidays.json` 节假日
   - `special_days.json` 特殊工作日
   - `YYYY-MM.json` 月度考勤
   - `YYYY-MM.leaves.json` 月度请假

   问题: 跨月查询要遍历目录、并发写有竞态、schema 无校验。

2. **采购订单**: 仅有事件级数据（inventory_events 里 event_type='purchase'，22,750 行），**没有订单主档**。供应商上传的 Excel 经 `purchase_service.py` 流式处理为 CSV，"哪次上传对应哪些 SKU"信息丢失，更没有「下单日期 vs 到货日期」双时间戳。

PG 迁移正好是规整时机，**一次性把所有数据持久化层统一到 PostgreSQL**。

**1.6.1 员工 + 考勤进 DB（2-3 天）**

新增 4 个 ORM 表：

```python
class Employee(Base):
    __tablename__ = "employees"
    employee_id: Mapped[str] = mapped_column(Text, primary_key=True)
    name: Mapped[str] = mapped_column(Text, nullable=False)
    role: Mapped[str | None] = mapped_column(Text)
    hire_date: Mapped[str | None] = mapped_column(Text)
    active: Mapped[int] = mapped_column(Integer, default=1)
    notes: Mapped[str | None] = mapped_column(Text)

class AttendanceRecord(Base):
    __tablename__ = "attendance_records"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    employee_id: Mapped[str] = mapped_column(Text, ForeignKey("employees.employee_id"))
    work_date: Mapped[str] = mapped_column(Text, nullable=False)  # YYYY-MM-DD
    status: Mapped[str] = mapped_column(Text)  # present / absent / half_day ...
    work_hours: Mapped[float | None] = mapped_column()
    notes: Mapped[str | None] = mapped_column(Text)
    __table_args__ = (
        UniqueConstraint("employee_id", "work_date"),
        Index("idx_attendance_date", "work_date"),
    )

class LeaveRecord(Base):
    __tablename__ = "leave_records"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    employee_id: Mapped[str] = mapped_column(Text, ForeignKey("employees.employee_id"))
    start_date: Mapped[str] = mapped_column(Text, nullable=False)
    end_date: Mapped[str] = mapped_column(Text, nullable=False)
    leave_type: Mapped[str] = mapped_column(Text)  # sick / vacation / personal ...
    approved: Mapped[int] = mapped_column(Integer, default=0)
    notes: Mapped[str | None] = mapped_column(Text)

class PublicHoliday(Base):
    __tablename__ = "public_holidays"
    holiday_date: Mapped[str] = mapped_column(Text, primary_key=True)  # YYYY-MM-DD
    name: Mapped[str] = mapped_column(Text, nullable=False)
    is_paid: Mapped[int] = mapped_column(Integer, default=1)
```

数据迁移脚本（一次性，spike）: 遍历 `attendance/*.json` 解析后批量写入。

**1.6.2 采购订单进 DB（3-4 天）**

新增 2 个 ORM 表：

```python
class PurchaseOrder(Base):
    __tablename__ = "purchase_orders"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    supplier_id: Mapped[str | None] = mapped_column(Text, ForeignKey("suppliers.supplier_id"))
    order_date: Mapped[str] = mapped_column(Text, nullable=False)        # 下单日期
    arrival_date: Mapped[str | None] = mapped_column(Text)               # 到货日期 (None=未到)
    status: Mapped[str] = mapped_column(Text, default="placed")          # placed/partial/arrived/cancelled
    source_file: Mapped[str | None] = mapped_column(Text)                # 原 Excel 文件名 (溯源)
    total_qty: Mapped[int] = mapped_column(Integer, default=0)
    total_amount: Mapped[float | None] = mapped_column()
    notes: Mapped[str | None] = mapped_column(Text)
    created_at: Mapped[str | None] = mapped_column(
        Text, server_default=text("CURRENT_TIMESTAMP")
    )

class PurchaseOrderLine(Base):
    __tablename__ = "purchase_order_lines"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    order_id: Mapped[int] = mapped_column(Integer, ForeignKey("purchase_orders.id"))
    product_barcode: Mapped[str] = mapped_column(Text, nullable=False)
    qty_ordered: Mapped[int] = mapped_column(Integer, nullable=False)
    qty_arrived: Mapped[int] = mapped_column(Integer, default=0)
    unit_price: Mapped[float | None] = mapped_column()
    __table_args__ = (
        Index("idx_pol_order", "order_id"),
        Index("idx_pol_barcode", "product_barcode"),
    )
```

`purchase_service.process` 流程改造:
1. 解析 Excel 后**先创建 `purchase_orders` 行**（status='placed', arrival_date=None）
2. 解析每条数据时同时写 `purchase_order_lines`
3. 后续到货时通过路由更新 `arrival_date` 和 `qty_arrived`
4. 「到货录入」时同步写 `inventory_events`（event_type='purchase'）保持向后兼容

**关键解锁: Lead Time 实测**

有了 `order_date` 和 `arrival_date` 双时间戳后:

```python
def compute_supplier_lead_time(supplier_id: str) -> dict:
    """返回该供应商最近 N 个订单的 LT 分布."""
    rows = session.execute(
        select(PurchaseOrder.order_date, PurchaseOrder.arrival_date)
        .where(PurchaseOrder.supplier_id == supplier_id)
        .where(PurchaseOrder.arrival_date.isnot(None))
        .order_by(PurchaseOrder.order_date.desc())
        .limit(50)
    ).all()
    lts = [(parse_date(a) - parse_date(o)).days for o, a in rows]
    return {
        "n_samples": len(lts),
        "median_days": median(lts) if lts else None,
        "p90_days": percentile(lts, 90) if lts else None,
    }
```

→ **直接解锁论文 §8.4 「补货决策」的入门**（reorder point = LT × 周需求 + safety stock）。

**1.6.3 测试改造（2 天）**

- `tests/test_attendance_service.py` ~40 测试改造为 ORM-based
- 新增 `tests/test_purchase_order_persistence.py` ~15 测试
- 现存 JSON 文件作为合成测试 fixture 保留（验证数据迁移正确）

### 论文贡献

加入「§7.5 数据持久化层规整」章节，~3000 字：
- §7.5.1 SQLite → PostgreSQL 迁移动机
- §7.5.2 schema 改造点（特别是 SQLite 专属语法）
- §7.5.3 数据迁移流程（pgloader + JSON → DB 迁移）
- §7.5.4 员工 + 考勤数据从文件系统迁移至 DB
- §7.5.5 采购订单主档化与 Lead Time 实测能力
- §7.5.6 性能对比（之前 vs 之后单 SKU 回测耗时）
- §7.5.7 部署架构图（含 postgres service）

第 6 章新增子节: 加 LT 实测内容可启动 §6.9 「基于实测 Lead Time 的补货决策初探」(将 plan §8.4 后续工作前置为正文)。

### 可能的坑

- 🔴 **pgloader 处理 BLOB / 大文本可能慢**：本项目几乎全是 INTEGER / TEXT，问题不大
- 🟡 **PG 时区处理跟 SQLite 不同**：所有 `event_at` 是 TEXT 'YYYY-MM-DD'，迁过去后还是 TEXT，反而最简单（但论文得提一句"为什么不用 PG 的 TIMESTAMP WITH TIMEZONE"）
- 🟡 **alembic 在两种 dialect 下都跑过**：需要 `op.get_bind().dialect.name == 'sqlite'` 守护 SQLite 特定语法
- 🟢 **回测 long-running 任务**：PG 跑 6000+ SKU 的 walk-forward 会比 SQLite 慢一些（PG 连接 overhead 大），但可接受

### 退场标准（实际勾选 @ 2026-05-30）

> 原文用 ✅ 表示「计划达成态」；下面用 ✅/⬜ 标注真实完成情况。

- ✅ 测试全过（已迁 `app/` 结构，含员工/采购新测）
- ✅ alembic upgrade head 在 PG 上跑通
- ✅ Coolify 部署 PG（shared PostGIS 17）+ 应用容器都健康
- ✅ `attendance/*.json` 全部迁入 DB（employees / attendance_records / leave_records / public_holidays，外加 special_days / inactive_periods 共 6 表）
- ✅ 采购订单主档化跑通：`purchase_orders` + `purchase_order_lines` 已建表并接入上传流程
- ✅ `compute_supplier_lead_times()` 函数已实现
- ⬜ 真 DB sanity：4 个 SKU `base_demand_view` PG vs SQLite 结果一致性 — 待补跑验证
- ⬜ 全量 v3 backtest（PG 版本）跑通，分数 vs v2 ≤5% 漂移 — 待跑
- ⬜ LT 实测输出 ≥5 个供应商 median LT — 函数就绪，但依赖线上采购数据（本地 PG 派生列为空），待线上验证
- ⬜ 论文 §7.5 章节（7 个子节）落档 — 移到 Phase 5 论文阶段

---

## Phase 2 (2026-06): 算法扩展 — Holt-Winters + TSB  ← 前移自 07

### 故事背景

v1/v2 已对 4 个 baseline 做完对比，结论是 NaiveSeasonal52W 最优。但 4 个 baseline 都是「最朴素」的方法。论文目前的批评点：

> "为何不试 Holt-Winters？这是经典的季节性方法，正好补 NaiveSeasonal52W 的不足。"

> "CrostonSBA 是 1972 年的方法，2011 年 Teunter-Syntetos-Babai (TSB) 已证明在 dying SKU 上表现更好。你既然发现了 dying 问题，为什么不直接换 TSB 试试？"

两个评委的疑问都有道理。Phase 2 把这两个补上，让算法对比更完整。

### 主要工作

#### 2.1 Holt-Winters（2 周）

- 引入 `statsmodels` 依赖
- 实现 `HoltWintersModel(season_periods=52)`，需要 ≥ 104 周（2 年）历史
- 对 ≥ 104 周的 SKU（项目里只有 402 个，1.3%）才启用
- 短历史 SKU 退化为 NaiveSeasonal52W
- 加入 walk_forward + 评分

#### 2.2 TSB（1 周）

Teunter-Syntetos-Babai 算法 [3]:

$$
\hat{p}_t = \alpha \cdot \mathbb{1}\{y_t > 0\} + (1 - \alpha) \hat{p}_{t-1}
$$

$$
\hat{z}_t = \alpha y_t + (1 - \alpha) \hat{z}_{t-1} \quad \text{(仅在非零时更新)}
$$

$$
\hat{y}_t = \hat{p}_t \cdot \hat{z}_t
$$

关键差异：TSB 的「需求概率」$\hat{p}$ **每周都更新**（不像 Croston 只在非零事件更新），所以对最后非零事件之后的零周敏感 — **理论上能解决 dying 问题，对照实验值得做**。

#### 2.3 对比实验（1 周）

- v4 backtest：6 个模型 × 1 view = 6 个 run
- 论文 §6.7 新增「HW vs Naive / TSB vs CrostonSBA」两组对比表
- 关键问：TSB 在 dying SKU 上能否不需要分流就工作？（用 v1 的过滤前数据测）

### 论文贡献

- §4.5 加 Holt-Winters 与 TSB 原理
- §6.4 baseline 表扩到 6 列
- §6.5.3 加「TSB 对 dying 的算法层修复 vs 本文数据层修复」对比讨论
- 答辩 PPT 加「6 模型 + 数据/算法层修复对比」幻灯

### 可能的坑

- 🔴 **statsmodels HW 在零值多的序列上不稳**：可能频繁报警告 / NaN，需要预处理（log+1 变换、季节剥离等）
- 🟡 **TSB 跟 SBA 共用 alpha=0.1 可能不公平**：需要 alpha 调优 spike
- 🟡 **statsmodels 单 SKU 跑 walk_forward 慢**：HW fit() 比 NumPy baseline 慢 10x+，6,098 SKU × 几百窗口可能 10h+

### 退场标准

- HoltWinters 6 个 unittest 过（边界 / 短历史 / NaN handling）
- TSB 6 个 unittest 过
- v4 全量 backtest 跑通
- 至少能讲出 1 个「TSB > SBA」的具体 SKU 案例

---

## Phase 3 (2026-07): ML 集成 — Prophet（可选 LSTM）  ← 前移自 08

### 故事背景

经典统计模型搞完后，评委会问：「ML 方法对比呢？」本科答辩通常不要求 ML，但**有 vs 没有**的论文档次差别明显。

Prophet 是首选：
- Facebook 出品，文档完善
- 适合零售业季节性
- 训练快、可解释（trend + seasonality + holidays）
- 不需要 GPU
- 集成 cost 低（1-2 天）

LSTM 是可选：
- 深度学习招牌
- 但训练慢、参数多、要 GPU
- 对小数据（156 周）容易过拟合
- **本科论文里出现「我们 fine-tuned LSTM」会被怀疑数据量是否合理**

### 主要工作

#### 3.1 Prophet 集成（1 周）

- `pip install prophet`
- 新建 `prophet_model.py`，实现 ForecastModel Protocol
- 关键点：Prophet 需要 DataFrame 输入 `ds` (date) + `y` (value)，要从 `dict[date, int]` 转
- 多步预测：Prophet 天然支持（make_future_dataframe），比 baseline 优势明显
- 节假日：可以加希腊节假日（已有 `public_holidays` 表）！这是 Prophet 的强项

#### 3.2 全量回测对比（3 天）

- v5 backtest: 7 个模型 × 1 view（加 Prophet）
- 关键问：Prophet 跟 NaiveSeasonal52W 谁更准？
- Prophet 通常在长历史 + 强季节 SKU 上明显胜出，短历史/间歇序列上可能不如 Croston

#### 3.3 节假日效应分析（2 天）

- 用 `public_holidays` 表把希腊节假日作为 regressor 加入 Prophet
- 跑 with-holiday vs without-holiday 两版
- 论文加「希腊节假日对预测精度的影响」章节

#### 3.4 LSTM（可选，3 周，仅作者 A 看时间允许）

- 用 PyTorch 简单 LSTM（不上 N-BEATS / Transformer 这种重的）
- 训练数据少（150 周 × 6000 SKU = 90 万样本）够用
- 实际 LSTM 在间歇序列上很难打过 Prophet + Croston
- **如果时间不够就跳过**，论文 §8.4 后续工作里列出来即可

### 论文贡献

- §4.6 加 Prophet 原理 + 实现细节
- §6.4 baseline 表扩到 7-8 列
- §6.8 新增「希腊节假日效应分析」章节
- 答辩 PPT 加「ML vs 经典 baseline」对比

### 可能的坑

- 🔴 **Prophet 安装在 Windows 上麻烦**（依赖 cmdstan），改用 Docker 跑或换 Linux
- 🟡 **prophet 单 SKU fit 比 baseline 慢 5-10x**：6,098 SKU 全跑可能要 2-3h
- 🟡 **LSTM 真做的话调参可能搞死人**：如果时间紧就 defer

### 退场标准

- Prophet 集成 + 6 个 unittest
- v5 全量 backtest 跑通
- 节假日 effect 分析有具体数字（如「春节后 1 周预测精度比无 holiday 模型高 X%」）

---

## Phase 4 (2026-08): 监控 + 多视图敏感性  ← 前移自 09

### 故事背景

到这时候算法面已经完备了，论文该补**工程深度**与**结论严谨性**。

### 主要工作

#### 4.1 view='all' 全量跑（1 周）

- 之前只跑了 view='base_demand'，没跑过 view='all' 对照
- v6 backtest: 4 baseline × 2 views (base_demand + all)
- compare_run_pair 输出 4 张对比报告
- 论文 §6.6 加 「base_demand 数据清洗的实际收益量化」章节

#### 4.2 dying 阈值敏感性（1 周）

- N=8 / 13 / 26 / 52 周 4 个阈值各跑一次 backtest
- 输出折线图：N → CrostonSBA Bias_avg 的关系
- 论文 §5.4.4 加敏感性分析子章节
- **预期发现**：N=13 不是全局最优，可能 N=8 更激进（少高估）或 N=26 更保守（少漏判）

#### 4.3 Prometheus + Grafana 监控（2 周）

部署级工程化：
- Flask 加 `prometheus-flask-exporter` 暴露 metrics
- backtest_service 加 metric: 每次 run 的 MASE_median / Bias_avg 写入 Prometheus
- Grafana dashboard 显示「过去 30 天每个 baseline 的精度趋势」
- 模型漂移告警：MASE 突然 +20% 自动告警

#### 4.4 自动备份（3 天）

- cron 每天凌晨 pg_dump → Hetzner Storage Box
- 保留 7 日 + 月度归档
- 写灾难恢复 runbook

### 论文贡献

- §6.6 数据清洗收益量化
- §5.4.5 dying 阈值敏感性分析
- §7.4 监控架构
- §7.5 数据备份策略

### 可能的坑

- 🟡 **Grafana 配置时间黑洞**：dashboard 美化容易耗 2-3 天，要克制
- 🟢 view='all' 跑可能比 base_demand 慢（SKU 多），但不会失败

---

## Phase 5 (2026-09): 论文终稿 + 答辩准备  ← 前移自 10（含 Phase 1 §7.5 补写）

### 故事背景

到这一步，所有实验都做完了，剩下是**讲故事**。论文不只是「我做了什么」，更要回答「为什么这样做、做出来怎么样、还有什么没解决」。

### 主要工作

#### 5.1 论文终稿（2 周）

- 重写绪论，把半年实际经历（v1 → 发现 bias → dying 修复 → PG 迁移 → ML 加入）变成 narrative
- 第 8 章「总结与展望」根据实际完成度调整
- 图表全部用 matplotlib 重画一遍（不再用 ASCII 框）
- 引用扩到 25-30 篇
- 查重 + 格式调整
- 找导师 / 同伴 / 学长读 + 反馈循环 3 轮

#### 5.2 答辩 PPT（1 周）

20-25 分钟答辩典型节奏:
- 问题背景 2 min
- 数据特征 + 挑战 3 min
- 系统架构 3 min
- 核心创新（dying 检测 + 4 类 SKU 分类）5 min
- 算法对比实验 5 min
- 系统部署 2 min
- 总结 + 展望 3 min
- Q&A 5-10 min

每张 PPT 不要超过 5 行字，全用图。

#### 5.3 答辩 demo（1 周）

- 服务器上准备好稳定的 demo 环境
- 浏览器开 SKU 详情页 / backtest 结果对比 / 月度报告（同学准备）
- 录屏作为备份（万一答辩当天网络抽风）

#### 5.4 同学协作（持续）

- 第 4 章作者 B 主笔，需要持续 review
- 8 个共同章节双人迭代
- 答辩分工最后定稿

### 论文贡献

- 终稿 25,000-30,000 字
- 12-15 张图（matplotlib + draw.io）
- 答辩 PPT 25-30 页

### 可能的坑

- 🔴 **导师反馈循环耗时长**：要提前 1 个月给导师，预留 3-4 轮修改
- 🟡 **写作 vs 实验 vs 部署 vs 同学协作 并行容易乱**：建议 Phase 5 把所有非答辩工作（数据 / 实验 / 部署）冻结

---

## Phase 6 (2026-11 - 12): 答辩 + buffer

最后预留 1 个月给：
- 突发状况（生病、家事、电脑挂了）
- 答辩反馈后的论文修改
- 学校行政流程（提交、查重、盲审等）

---

## 风险表

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 同学（作者 B）拖延章节 | 中 | 高 | 6 月底前必须给同学 deadline 写出第 4 章初稿 |
| ~~PG 迁移踩坑~~ | — | — | ✅ 已完成（走 Coolify shared PG），未踩 collation 坑 |
| ~~同学拒绝迁 PG~~ | — | — | ✅ 已完成，风险消解 |
| Prophet / LSTM 在 Windows 上装不上 | 中 | 中 | 准备 Linux VM / WSL 备份 |
| 客户在毕设期间更新 ERP 导致数据 schema 变 | 低 | 高 | 跟客户协商「答辩前 schema 冻结」 |
| Hetzner 服务器宕 | 低 | 中 | pg_dump 每日备份（待配）+ 本地保留全量 DB 副本 |
| 派生列/采购数据缺失致 LT 实测无样本 | 中 | 中 | 先确认线上采购订单是否有 arrival_date，必要时补录历史到货日期 |

---

## 后续可能展开（写不进毕设但值得记）

如果毕设答辩完后这个项目还想继续做，下面这些方向值得：

1. **跨企业泛化**: 把 dying 阈值 / 4 类分类规则在第二家公司数据上验证，看是否需要重新校准
2. **真实补货决策**: 现在只做预测，没做「该不该补、补多少」。加 reorder point / safety stock 计算（基于回测精度反推合理 lead time 内的安全库存）
3. **多语言前端**: 现在中希混排，可以正式做 i18n 提升对希腊员工友好度
4. **API 商业化**: 把预测 API 包装成 SaaS（每月几欧元 per 千 SKU），其他希腊小零售可订阅
5. **学术发表**: 这个 dying 检测 + 4 类分类思路写成 short paper 投 *Forecasting* journal 或 IFORS 会议

---

## 写在最后

「半年时间」听起来很多，实际除掉课程考试、节假日、生病、家事、答辩准备本身，实际能用来推进项目的可能只有 3-4 个月。

本路线图的核心原则：

1. **先做能直接对论文加分的事**（Phase 1 PG 迁移、Phase 2 算法扩展、Phase 4 敏感性分析）
2. **风险大、收益不确定的事放后面**（LSTM、跨企业泛化）
3. **每个 Phase 最后一周冻结代码做写作**（不要算法和论文同时改）
4. **跟同学的 sync 节奏每两周一次**（不然到 Phase 5 才发现第 4 章没写完）

走起。

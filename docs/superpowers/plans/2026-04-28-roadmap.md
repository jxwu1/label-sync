# 项目演进路线图

> **用途**：把"标签处理工具 → 数据分析平台"这条长期路线上的决策、待办、依赖关系
> 集中存档，供下次开新会话时一眼看清当前位置。每完成一项就把 `[ ]` 改成 `[x]`。

**起草日期**：2026-04-28
**最后更新**：2026-04-28
**当前所在阶段**：第 1 期（货号历史）即将合并 → 准备进入"阶段 0 quick wins"

---

## 大图（路径地图）

```
当前 main
   │
   ├─ feature/model-history    ← 第 1 期（货号历史 + admin 清理），待合并
   │
   ├─ refactor/quick-wins      ← 阶段 0：Calamine + WAL（半天）
   │
   ├─ refactor/sqlalchemy      ← 阶段 1：ORM + Alembic（1-2 周，分多个 PR）
   │
   ├─ feature/multi-location   ← 阶段 1.5：多库位 schema（依赖阶段 1）
   │
   ├─ refactor/alpine          ← 阶段 2：前端响应式（与后端解耦，可并行）
   │
   ├─ feature/scan-history     ← 第 2 期：扫描历史浏览（output/ 下钻）
   │
   ├─ feature/inventory-events ← 第 3 期：进出货事件
   │
   └─ feature/sales-analysis   ← 第 4 期：销售分析 dashboard
```

每条横线是独立分支 / PR。不要并行做两条互相依赖的。

---

## 第 1 期收尾（feature/model-history → main）

### 必做

- [x] 浏览器手测 5 tab + FAB + 采购页月度采购总结回归正常（用户操作）
- [x] 合并 `feature/model-history` 到 main（72c8c5c）

### 可选（见下面"决策日志"5）

- [x] 当前状态把 `stockpile_location` 拆成"店面位置 / 仓库位置"两列展示（只读，不改 schema） — done in d9120bd，时间线保留原字符串形式（YAGNI）

---

## 阶段 0：quick wins（refactor/quick-wins）

完成节奏：单分支单合并，半天到一天。

- [x] 安装 `python-calamine`，5 处 `pd.read_excel(..., engine="calamine")` —— 当前仓库无大 xlsx，benchmark 待 10MB 文件到位后补
- [x] **追加**：CSV 主路径加 `engine="pyarrow"`（`file_io.py`）；5MB / 43750 行 benchmark 显示 1.02x（C-engine 已足够快），保留以兜未来更大文件
- [x] `stockpile_db.py:_connect()` `PRAGMA journal_mode=DELETE` → `WAL`
- [x] 跑全套 pytest 验证零回归（194/194 通过）
- [x] 合并

**理由**：纯收益的两个改动，零风险，做完先享受 1-3 个月，再开始大重构。

**决策记录（2026-04-28 实施）**：CSV pyarrow 是这次新增的一项，原 plan 没列。原因：用户实际"大文件"基本是 csv 不是 xlsx，calamine 只覆盖 xlsx，pyarrow 对偶覆盖 csv。在 5MB 上无显著差异，但 fallback 路径保留默认 C-engine，零回归。

---

## 阶段 1：数据层 ORM 化（refactor/sqlalchemy 系列）

完成节奏：拆成多个 PR，每个 PR 单独可合并、可回滚。**期间业务功能开发不停**。

### 1.0 准备（refactor/sqlalchemy-base）

- [x] 装 SQLAlchemy 2.0.49 + Alembic 1.18.4
- [x] `models.py`：`automap_base` 反射 stockpile / stockpile_changes / schema_meta，导出 `Stockpile` / `StockpileChange` / `SchemaMeta` 类 + `get_session` / `get_engine`
- [x] `alembic init alembic` + 配置 env.py 用 `Base.metadata` + `alembic revision -m "baseline"`（空 upgrade/downgrade）+ `alembic stamp head` → 现 DB 已写入 `alembic_version` 表，revision = `cf04ed0496f7`
- [x] `tests/test_models_smoke.py`：5 个 case 覆盖 SELECT/INSERT/is_active int/默认时间戳/changes 表，全过
- [x] 合并

### 1.1 stockpile_db ORM 化（refactor/sqlalchemy-stockpile）

最重的模块，单独一个 PR。

- [x] 用 ORM 重写 `_upsert` / `_log_change` / `_sync_export_dataframe` / `_deactivate_missing_records`，**read 路径也一并 ORM 化**（count / query_by_barcode / query_all_* / list_inactive_records / list_changes / search_stockpile）
- [x] 保留所有函数签名不变（外部调用者无感）
- [x] 已对比 SQL 行为：is_active 用 `_ACTIVE=1` / `_INACTIVE=0` 常量与 INT 比较；`updated_at` 在 UPDATE 时显式设 `func.datetime("now","localtime")`；INSERT 不传 created_at/updated_at 让 DB 默认 fire；`extra` 仍 `json.dumps/loads` 手工
- [x] `tests/test_stockpile_db.py` 31/31 通过；总测试 199/199
- [x] **benchmark**（43495 行全表扫描，5 轮平均）：raw 70ms / ORM 3 列 tuple 85ms（1.22x） / ORM 完整对象 320ms（4.62x）。生产 read 路径走 3 列 tuple 几乎无感；完整对象路径仅 limit=100 / per-import 使用，绝对值仍小
- [x] 合并

**1.0/1.1 实施备忘**：

1. SQLite reflect `INTEGER PRIMARY KEY AUTOINCREMENT` 列得到 `nullable=True`，会触发 SA 2.0 sentinel-column 检查报错。`models.py` 显式 `Stockpile.__table__.c.id.nullable = False`（StockpileChange 同）修复
2. `Session(engine)` 用 `expire_on_commit=False`，commit 后属性仍可读，便于 `_stockpile_to_dict`
3. 引擎用 `NullPool`：避免 Windows 测试 tearDown 时 SQLite 文件锁
4. `stockpile_db.py` 现在 448 行（>250 软上限），含 ~70 行旧 schema bootstrap（`_SCHEMA` / `_migrate_schema` / `_ensure_schema` / `_read/write_schema_version`）。1.3 阶段会被 Alembic 替代，届时回到 ~380 行

### 1.2 其它 service 逐个 ORM 化（每个 service 一个 PR）

实施时发现：除 history_service 外，其它 service 都通过 `stockpile_db.*` 公共 API
访问数据，已在 1.1 间接 ORM 化。本阶段实际仅一个 PR：

- [x] `history_service.py` —— `find_record` / `aggregate_events` 两处 SELECT 切到
  `stockpile_db._session()` + `select(Stockpile/StockpileChange)`
- [x] `attendance_service.py` —— 不接触 stockpile DB（自有 attendance/ 数据），跳过
- [x] `monthly_summary_service.py` —— 不接触 stockpile DB（自有 monthly_summary/），跳过
- [x] `purchase_service.py` —— 仅调 `stockpile_db.insert_or_update`，1.1 已间接 ORM
- [x] `query_service.py` —— 不接触 stockpile DB，跳过

**1.2 实施备忘**：

1. **automap → 声明式切换**：1.0/1.1 用的 automap_base 在 import 时强依赖 stockpile.db
   存在并已建表，与测试 fixture 冲突（fixture 在 import 前已 patch CONFIG 指向空 tmp_path）。
   1.2 改成 `DeclarativeBase` + 手写 `Stockpile` / `StockpileChange` / `SchemaMeta` 类。
   schema 与 stockpile_db._SCHEMA 字符串保持手动一致；1.3 删 _SCHEMA 后由 ORM metadata 单源
2. **alembic check 当前有 metadata 层面差异**（TEXT vs String、缺 Index、缺 server_default
   等）—— 实际 DB schema 是正确的，差异在 ORM 类与 schema 的对齐度。1.3 阶段统一
   处理：让 metadata 与实际 DB 完全一致后 alembic 即可干净 autogenerate

### 1.3 收尾（refactor/sqlalchemy-cleanup）

- [x] 删除 `_SCHEMA` 字符串 / `_ensure_schema` / `_migrate_schema` / `_read_schema_version` / `_write_schema_version`，schema 单源迁到 `models.py:Base.metadata`
- [x] `ensure_db()` 改走 `Base.metadata.create_all(engine)` + `_bootstrap_schema_version()`（idempotent ORM upsert）
- [x] `_connect()` 自动调 `ensure_db()` 兜底，调用方无感
- [x] `models.py` metadata 与实际 DB schema 完全对齐（Text 而非 String / 3 个 Index 显式声明 / `server_default=text("(datetime('now','localtime'))")` 等）
- [x] 删 `schema_meta` 表保留（routes_stockpile `/schema` endpoint 仍在用）；保留 `SchemaMeta` ORM 类
- [~] **`sqlalchemy-continuum` / event listener 自动维护 stockpile_changes** —— roadmap 列了但实属 nice-to-have，本阶段未做。`_log_change` 仍由 `_upsert` 显式调用，行为可控且测试好写
- [x] 文档：`使用文档.md` 加 "开发：如何加新 schema 字段" 段，给 5 步流程
- [x] 删 `tests/test_stockpile_db.py::test_migrate_legacy_db_*`（legacy ALTER TABLE 路径已不存在）

**1.3 实施备忘**：

1. **alembic check 仍报 3 个 PK NOT NULL diff**（schema_meta.key / stockpile.id / stockpile_changes.id）。原因：现 DB 里这些列反射出 `nullable=True`（SQLite 老语义：PK 不强制 NOT NULL），ORM 类声明的是 `nullable=False`（语义正确）。SQLite ALTER 不支持直接改 nullable，需要 `op.batch_alter_table` 重建表 + 复制 43k 行数据
2. **延后到阶段 1.5**：1.5 多库位 schema 改造时无论如何要重建 stockpile 相关表，新表直接 NOT NULL PK 落地，顺手解决；现在不动数据
3. autogenerate 出来的临时迁移文件 `b3ec13e4378e_*.py` 已删除（避免误执行）

---

## 阶段 1.5：多库位 schema 改造（feature/multi-location）

**前置依赖**：阶段 1 完成（必须）。**没 ORM + Alembic 之前不要碰这个**。

### 决策已定（2026-04-29 会话）

1. **业务真相**：用户确认两类多库位场景
   - Case 1：旧位 + 新位过渡（不确定老位是否清空，保留两者）
   - Case 2：溢出多位（如 A14-12-01 + A14-12-02 同款多个货架）
   schema 不区分这两种，都用"一货号 N 库位"模型

2. **schema 选型**：**(a) 一对多子表**
   - `stockpile_locations(stockpile_id FK→CASCADE, location, kind, position, created_at)`
   - UNIQUE(stockpile_id, location)
   - 主表 `stockpile.stockpile_location` **永久保留**作月度比对的字节级源
   - 子表是 raw 字符串的派生分析视图

3. **历史噪音处理**：**观察 ≠ 修改**。**不写清理脚本**
   - 老系统的 round-trip / 空格噪音可能是真实业务事件，删了就丢证据
   - 改成"数据质量分析页"列出异常给用户看，老系统改完下次 import 自动同步过来

4. **比对功能保护**：用户提出顾虑——修复期内 cosmetic 不一致会淹没 substantive 真错误
   - 解决：**双轴比对**。compare 输出 cosmetic_mismatches + substantive_mismatches 两类
   - normalize 规则：每段独立 strip，**保留段顺序**（店面在前/仓库在后是契约）
   - substantive ≥ 3 触发显眼告警
   - 加 stockpile_snapshots 表，每次 import / compare 留计数快照供趋势分析

### 实施（拆 4 个 PR，全部完成）

- [x] **PR1** `feature/multilocation-subtable` (df310c2) —— 加子表 + alembic 数据迁移 +
  dual-write。43,495 行 stockpile_location 字符串解析填到 40,671 子表行
  （31,766 store + 8,888 warehouse + 17 unknown）
- [x] **PR2** `feature/multilocation-readpath` (f42d41f) —— history_service 当前状态
  读子表 + unknown 列前端展示；`split_location` 升级输出 unknown
- [x] **PR3** `feature/data-quality-page` (7f68945) —— 数据质量分析页（顶层
  "🔍 数据质量" tab）。4 段：multi_same_kind / flippers / whitespace_anomalies /
  unknown_prefix。**只展示，不删**。实测真 DB：113 / 81 / 1005 / 17
- [x] **PR4** `feature/two-axis-compare` (be7eadf) —— 双轴比对：
  `compare_with_dataframe` 拆 cosmetic_mismatches / substantive_mismatches +
  alert flag；新增 stockpile_snapshots 表 + `/stockpile/snapshots` endpoint；
  前端比对页拆两段（红框实质 / 黄框 cosmetic）

测试 198 → 221（+23 新 case）。alembic head: cf04ed0496f7 → 2385c879eb58 → c80afeb49bae。

### 已知遗留

- alembic check 仍报 3 个 PK NOT NULL cosmetic diff（schema_meta.key / stockpile.id /
  stockpile_changes.id）。需要 batch_alter_table 重建表才能修，留给未来真正 schema
  改动时一并解决（比如真正合并主表 stockpile_location 列）
- HM-* 异常前缀 17 条：用户决定加进 STORE_PREFIXES 还是保留为 unknown 待定

---

## 阶段 2：前端响应式（refactor/alpine）

**与阶段 1 互不依赖**，可并行。但**不要在多库位改造同期做**，避免冲突。

- [ ] 装 Alpine.js（CDN 起步，后期换 npm + Vite）
- [ ] 把 `static/js/index.js:switchPage` 那段硬编码 pageMap/navMap 改成 Alpine `x-data` 响应式绑定（消除 4-28 那个"漏配 history"的 bug 类）
- [ ] FAB 抽屉（transfer / terminal）改用 Alpine
- [ ] 货号历史页用 Alpine 重写（搜索状态、隐藏面板）
- [ ] 各 tab 内的小交互逐步迁移
- [ ] 评估：是不是要引入 Vite 做 ES module bundle

---

## 阶段 3：第 2 期 扫描历史（feature/scan-history）

**前置依赖**：阶段 1 完成（强烈建议），阶段 1.5 完成（如果要把扫描批次和库位变更交叉分析）。

- [ ] 在 货号历史 tab 内加二级 tab "📂 扫描批次"
- [ ] 后端：`output/{员工}价格标{时间戳}/` 文件夹扫描，给一份"批次列表 + 批次内容"API
- [ ] 前端：员工列表 → 该员工批次时间线 → 该批次扫描内容（CSV 内容）三层下钻
- [ ] 与库位变更时间线交叉显示：例如某批次后某货号库位变更可视化连接

---

## 阶段 4：第 3 期 进出货事件（feature/inventory-events）

**前置依赖**：阶段 1 + 1.5 完成。

### 决策待办

- [ ] 数据来源：手动录入 / 扫发票 / 对接 ERP？
- [ ] 事件粒度：单笔 vs 按日聚合？
- [ ] 库存数推算 vs 单独存？

### 实施

- [ ] 新建 `inventory_events` 表（事件流）
- [ ] 录入 UI（货号历史 tab 内增加"📥 进出货"二级页）
- [ ] 单货号的事件时间线展示
- [ ] 进货 / 销售 / 补货事件类型 schema

---

## 阶段 5：第 4 期 销售分析 dashboard（feature/sales-analysis）

**前置依赖**：阶段 4 数据积累 ≥ 1 个月。

- [ ] 引入 ECharts
- [ ] 引入 Clusterize.js（如果要做"全部货号列表浏览"）
- [ ] dashboard 设计：畅销品 / 滞销品 / 周转率 / 库位空闲率
- [ ] 引入 Tailwind（仅 dashboard 新页面，不动旧页面）

---

## 横切技术债（任何阶段都可以挑出来做，单独分支）

- [ ] **Pydantic 做 API 边界校验**（替代 routes 里手写 `if not data.get(k)` 那一堆）
- [ ] **Frontend bundler**（Vite 或 esbuild）—— Tailwind / Alpine 的 npm 化前置
- [ ] **pre-commit + ruff + black + eslint** —— 代码风格与 lint 闸门
- [ ] **Playwright 或 Vitest 前端测试** —— 当前 0 个前端测试
- [ ] **`stockpile_db.py:_log_change` 替换为 SQLAlchemy event listener** —— 阶段 1.3 的子任务，但也可独立
- [ ] **`config.py` 里 `dual_mode` 重命名为 `enable_transfer`**（语义已偏离）
- [ ] **`垃圾桶/` 文件夹清理**（应该 .gitignore 而不是入库）

---

## 决策日志（2026-04-28 会话）

按时间顺序记录这次会话产生的关键决策，未来想质疑某个选择时回看：

1. **路线分期**：长期路线拆成 4 个独立子系统（A 货号历史 / B 扫描历史 / C 进出货事件 / D 销售分析），每期独立 spec/plan/分支。**不做"先把所有数据建模做完再开始"那种瀑布**。
2. **第 1 期范围**：只做"货号历史搜索 + 库位变更时间线"，**只读**。明确不做：模糊搜索、就地编辑、扫描批次、销售事件、销售分析。
3. **聚合算法**：5 秒窗口聚合（基于实际数据时间间隔分布：0-5s 占 56%、6-60s 0%、>1min 44%）。
4. **轮回噪音**：发现 927 条 stockpile_changes 里 65%（602 条）是 import 流程的来回切噪音，根因在 `_sync_export_dataframe` 没去重。**本期不修**，留给阶段 1.5。
5. **多库位真相揭示**：`stockpile_location` schema 实际是"店面/仓库"两段编码（A/B/C 开头是店面，X/Z 开头是仓库），但 `parse_locations` 不允许同维度多个。用户确认业务上**真的存在**同时两个店面/两个仓库的货号，**当前 schema 表达不了**。需要阶段 1.5 重做。
6. **admin 清理**：`/admin` 控制台页 + `/stats` 月度扫描柱状图全删（用户从未实际使用 /admin，互传已通过 A 端 FAB 抽屉解决）。保留：`routes_collab` / `routes_transfer` / `routes_monthly_summary` / `dual_mode` 配置（A↔A 互传依赖）。
7. **技术栈重构方向**：用户提议 Tailwind + Alpine + ECharts + Clusterize + Flask + SQLAlchemy + WAL + Calamine。评估结论：**80% 价值集中在 SQLAlchemy + Alembic**（直接解决 schema 演进痛点）；其余各自有价值但不解决当前痛点，按需引入。**不一次性大爆炸重构**，分阶段。
8. **重构对现有功能的影响**：渐进迁移，逐模块 ORM 化，每个 PR 保持函数签名不变。**功能层面应该 0 影响**，但要提防 SQL 行为细微差异（is_active 1/True 比较、datetime 默认值、extra JSON 列）和性能回归（ORM 比 raw 慢 2-3x，热路径要 with_entities 优化）。
9. **第 1 期收尾**：选 (A) 当前分支用现有技术栈合掉，重构在新分支独立做。不在当前分支扩范围。

---

## 这份文档怎么维护

- **新决策**：补在"决策日志"末尾，**不要**修改历史条目（保留思路演进的轨迹）
- **新发现的待办**：补到"横切技术债"或对应阶段的待办列表
- **完成一项**：把 `- [ ]` 改成 `- [x]`，**不删除**（已完成项是历史进度证据）
- **某阶段大改方向**：在该阶段标题下加 `> 2026-XX-XX：方向调整为 ...`，旧待办划掉但保留
- **下次新会话开始**：先读这份 + `docs/superpowers/specs/` 下最新 spec，再决定下一步

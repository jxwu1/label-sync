# GitNexus 使用范围（本项目覆盖默认）

上面 `gitnexus:start/end` 段是 `npx gitnexus analyze` 自动生成，每次重索引会被覆盖。**实际工作规则以本段为准**——本段位于标记块外不会被自动重写。

## 缩窄的 MUST 场景（覆盖 "Always Do" 第一条）

只有以下场景必须先调 `mcp__gitnexus__impact`：

1. 重命名 / 删除 函数、类、方法
2. 跨文件拆分或合并模块
3. 改动 public API 表面（HTTP 路由、Protocol 实现、被多处 import 的接口）
4. 不确定一个 symbol 有哪些调用方时
5. 批量重构（如 Phase 2 目录重组、Phase 7 PG 迁移）

**不需要** 跑 impact 的场景（之前规则覆盖过宽，导致心理负担过大而完全不用，反而违反原意）：
- 改文档 / 注释 / CLAUDE.md / README / `docs/`
- 改配置（`pyproject.toml`、`Dockerfile`、`.gitignore`、`.claude/`）
- 改测试用例（仅 add / update assertions，不动被测函数签名）
- 改 `_scratch/` 实验脚本
- 在一个已经清楚知道调用方的函数内部调整实现

## Commit 前

仍建议跑 `mcp__gitnexus__detect_changes` 验证改动范围。**项目级 post-commit hook**（`.claude/hooks/gitnexus-post-commit.cjs`）会在 `.py` 文件 commit 成功后后台触发增量重索引，所以即使忘了 detect_changes 也不会出现下次 session 拿到 stale index 的问题；同时 hook 会扫 transcript，若整 session 未调 detect_changes 会 warn（warn-only，不 block commit）。

钩子工作流：
- **触发**：`Bash` 工具调用结束 + 命令含 `git commit` + 退出码 0 + `git diff HEAD~1 HEAD` 含 `.py`
- **行为**：后台 `npx gitnexus analyze`（自带 HEAD 比对，已索引时 no-op）+ 可选 warn
- **不重复 commit**：`--amend` 跳过

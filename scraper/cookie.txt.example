# 把 boson ERP 的 PHPSESSID 粘贴到 cookie.txt (不带井号, 整个文件就一行)
# 步骤:
#   1. 浏览器登录 boson, F12 -> Application -> Cookies -> 复制 PHPSESSID 的值
#   2. cp cookie.txt.example cookie.txt
#   3. 把 cookie.txt 里这段全部删掉, 只留 PHPSESSID 那串
#
# cookie 失效信号: 抓取脚本响应 < 50KB 或返回 "系统已注销"
